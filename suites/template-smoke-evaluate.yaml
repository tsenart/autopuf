id: template-smoke-evaluate
mode: research
artifacts_root: artifacts/runs
search:
  algorithm: smoke
  max_iterations: 1
  seeds:
    - candidates/baseline-classical-crp-001.yaml
worlds:
  - configs/worlds/lab-clean-v1.yaml
attacks:
  - modeling
  - replay
  - nearest_match
  - crp_exhaustion
  - drift_abuse
scoring:
  file: configs/scoring/default.yaml
budgets:
  queries: 1000
  observations: 256
  search_steps: 128
