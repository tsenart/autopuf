# Plan

1. Create the minimal Lean package files and module layout.
2. Mirror the repo abstractions in simple formal data structures.
3. Add a local build README for the workspace.
4. Run `cd formal && lake build` and record the result.

